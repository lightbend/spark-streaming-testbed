* make the connection port and host configurable (streaming)
* make the spark connection port and host configurable (driver)
* add some computation hungry logic (hanoi towers?), and stats on the computation time